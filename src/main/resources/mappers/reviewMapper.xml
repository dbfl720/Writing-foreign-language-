<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
 
 
 <mapper namespace="com.language.review.dao.ReviewMapper">
 
 	<insert id="insertReview" parameterType="map">
 		INSERT INTO `review_writing`
 		(
 			`userId`
 			,`reviewCategoryId`
 			,`languageCategoryId`
 			,`title`
 			,`rating`
 			,`content`
 			,`ImagePath`
 			,`createdAt`
 			,`updatedAt`
 		)
 		VALUES
 		(
 			#{userId}
 			,#{reviewCategoryId}
 			,#{languageCategoryId}
 			,#{title}
 			,#{rating}
 			,#{content}
 			,#{ImagePath}
 			,NOW()
 			,NOW()		
 		)
 	
 	</insert>
 	
 	
 	
 	<select id="selectReviewListByLanguage" parameterType="String" resultType="com.language.review.model.Review">
 		SELECT
 			`id`
 			,`userId`
 			,`reviewCategoryId`
 			,`languageCategoryId`
 			,`title`
 			,`rating`
 			,`content`
 			,`ImagePath`
 			,`createdAt`
 			,`updatedAt`
 		FROM
 			`review_writing`
		WHERE
			`languageCategoryId` = #{languageCategoryId}
 		ORDER BY `id` DESC
 	</select>
 
 
 
 	<select id="selectReviewList" resultType="com.language.review.model.Review">
 		SELECT
 			`id`
 			,`userId`
 			,`reviewCategoryId`
 			,`languageCategoryId`
 			,`title`
 			,`rating`
 			,`content`
 			,`ImagePath`
 			,`createdAt`
 			,`updatedAt`
 		FROM
 			`review_writing`
 	
 	</select>
 	
 	
 	<select id="selectReviewCountByReviewIdUserId" parameterType="map" resultType="int">
 		SELECT
 			COUNT(*)
		FROM
			`review_writing`
		WHERE
			<if test="userId != null">
			`userId` = #{userId}
			</if>
		ORDER BY `id` DESC
 	
 	</select>
 	
 	
 	
 	<select id="selectReviewCountList" resultType="com.language.review.model.Review">
		SELECT
 			userId, count(userId)
 		FROM
 			`review_writing` group by 1
		ORDER by 2 DESC
 	
 	</select>
 
 </mapper>